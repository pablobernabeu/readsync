# Vendored MediaPipe runtime and model, fetched by scripts/fetch-assets.mjs.
# They are large binaries, not source, so they are not committed.
public/vendor/
public/models/*.task

# Tooling
node_modules/
.firebase/
